This project is an observability agent, should be invoked based on alerts generated by your observability tool.
The tool uses langchain to create a workflow stemming from the gcx workflows

We use gcx command line tool to request information about our services through the grafana backend.
Users will need to provide some credentials.
